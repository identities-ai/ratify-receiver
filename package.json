{
  "name": "@identities-ai/ratify-receiver",
  "version": "0.1.2",
  "description": "Fail-closed TypeScript receiver helper for Ratify Verify protected actions.",
  "license": "Apache-2.0",
  "private": false,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/identities-ai/ratify-receiver.git"
  },
  "homepage": "https://github.com/identities-ai/ratify-receiver",
  "bugs": "https://github.com/identities-ai/ratify-receiver/issues",
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --import tsx --test test/*.test.ts",
    "check": "tsc --noEmit && npm test",
    "prepack": "npm run build",
    "prepublishOnly": "npm run check"
  },
  "devDependencies": {
    "@identities-ai/ratify-protocol": "1.0.0-alpha.20",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "peerDependencies": {
    "@identities-ai/ratify-protocol": "1.0.0-alpha.20"
  }
}
